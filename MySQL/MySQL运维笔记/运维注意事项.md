#### MySQL

1.运维工具
```
notices :
运维MySQL数据库需要哪些工具，来支持朴实无华的操作。

scene :
1.查看业务量大小
2.预估资源使用情况
3.网络连通情况
4.可用性
5.表变更
6.数据变更
7. 

review :
1.监控架构：
基础监控：CPU，磁盘，QPS/TPS -> 趋势监控，便于资源的扩缩，最好能达到表级别趋势展示的地步，成为无用数据删除的依据。
警戒监控：存在堆积问题，在MySQL最有可能的是慢查询堆积，所以慢查询监控是最最有必要的，能解决90%的问题(不是开玩笑0.0)。图借鉴阿里云的模式就很不错，横坐标时间段，纵坐标是慢查询种类及各类数量。实现方式一般在于两种：1.performance_schema(参考MySQL监控->performance_schema) 2.网关流量复制解析。

```



2.即时中止
```
notices :  
从运维经历来看，在操作之前，预先准备及时中止的手段非常重要，除非你是真正意义上的专家。

scene :  
一次加索引，模糊意识认为只会在前后加一次写锁以同步元数据，如果写操作和查操作都很快是没什么影响的。当时是80G的表操作，跑了3分钟在预计之内，其实当时看监控应该能发现连接数线性上升，单连接打满时，已经很难处理了，因为开启DDL的客户端无法中止，后面进行了主从切换已减少业务阻塞。

review :  
回过头想想，执行操作前开启另一个客户端进行及时中断可以防备该类问题。再看连接数上升的原因是，大表存在select * 全表查询，持续时间长达几个小时，查询MDL_SHARED_READ锁与DDL变更一阶段MDL_SHARED_NO_WRITE锁兼容，业务插入语句被阻塞，程序端不断开启线程，造成数据库实例连接数打满。如果能在另一边打开监控，观察到情况，及时地用已经连接的客户端线程去中断操作，将减少对生产业务的影响。
```

#### WEB开发
1.前后分离
```
notices :  
在人手不够和小团队开发无技术领导的情况下，不要使用前后端分离分配工作，这样会扼制创造性。

scene :  
背景是在运维团队下开发一个平台，分配为后端开发，所以每个需求都是必须要和前端配合才能做出尝试性开发，配合不好非常影响工作效率和积极性。

review :  
想到的解决是：不区分前后端开发，可以使用分需求开发git分支，各自管控自己的分支代码，在合并时可以一起讨论，解决冲突。

```
